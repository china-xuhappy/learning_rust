// 可以注册， 可以加 条目 金额， 可以查询（需要认证）

struct UserModel {
  username: String,
  phone: String,
  sign: String, // 获取数据 - 签名
}

impl UserModel {
  // 用户注册
  fn register(user: &UserModel) -> bool {
    
    
  }
}

